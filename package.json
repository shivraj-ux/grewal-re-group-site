{
  "name": "grewal-re-group-site",
  "version": "1.0.0",
  "private": true,
  "description": "Grewal RE Group — static site plus an MCP server (Netlify Function) for AI agents.",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "zod": "^3.23.8",
    "zod-to-json-schema": "^3.23.5"
  }
}
