# Files that must never be deployed to production.
# The build command in netlify.toml also deletes these, but .netlifyignore
# is a second layer — it prevents deployment even if the build command is
# skipped (e.g., manual drag-and-drop deploy or a failed build step).

CLAUDE.md
data/competitor-log.json
data/seo-config.json
data/site-audit.json
blog/_TEMPLATE_ENHANCED.html
archive/
